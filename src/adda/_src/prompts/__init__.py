"""Prompt-side text assembly: system prompts, the tool catalog, and the math DSL.

Groups the agent system-prompt constants, the tool-catalog renderer that
turns tool-closure docstrings into an agent's ``<tools>`` prompt section,
and the math expert's symbolic-derivation DSL.
"""
