"""Run orchestration: the agent graph, its shared state, and the run loop.

Groups the pieces that build and drive one agentic run — graph assembly,
the ``AgenticState``/settings/phases it carries, the top-level ``AgenticRun``
entry point, the f3dasm-facing optimizer adapter, and the run diagram.
"""
