# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/

# venvs / tooling
.venv/
.venv-*/
.uv/
uv.lock

# tests / coverage
.pytest_cache/
.hypothesis/
.coverage
coverage_html_report/
htmlcov/

# docs build
site/

# OS / editor
.DS_Store
.idea/
.vscode/

# local agent instructions (personal working contract, not shared)
CLAUDE.md
CLAUDE.local.md
.claude/

# --- repo hygiene (managed) ---
.DS_Store
Thumbs.db
.sesskey
__pycache__/
*.py[cod]
.venv/
venv/
node_modules/
build/
dist/
*.egg-info/
.vscode/
.idea/
*.sqlite
*.sqlite3
.context/
/AGENTS.md
/GEMINI.md
/STATUS.md
/PLAN.md
/*_SUMMARY.md
/HANDOFF*.md

# Run artifacts. Each run's debug/delegations/ is its OWN git repository
# (spec 11: one commit per delegation), so this is not merely noise-filtering:
# without it a `git add -A` here would absorb that nested repo as a gitlink
# and commit a broken submodule reference into adda.
studies/*/runs/
